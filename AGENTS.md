# 项目协作约定

本项目当前处于需求发现和技术路线评审阶段。未经方案评审，不把研究结论写成已批准的实现决定。

- 仓库本身必须让人读懂：README 给出目标、当前阶段和三篇主阅读入口；每个现行主题只保留一处详细说明，其他页面链接过去。先用具体用户任务解释设计，再给字段和协议；文件名、编号、链接和状态保持一致。
- 中文文档使用直接、具体的表达。事实、推断、建议、待验证项分开说明。
- 研究以官方文档、上游源码和可复现实验为依据，记录 URL、版本或 commit、访问日期及验证范围。
- `docs/product/` 保存需求与用户故事；`docs/architecture/` 保存架构候选及 ADR；`docs/research/` 保存调研；`docs/engineering/` 保存研发流程。
- 外部源码只放在 `research/upstreams/`，上游保持自己的 Git 历史；主仓库追踪锁定清单和获取脚本，不重复提交第三方整仓。
- 实验只放在 `research/spikes/`。自编实验代码、锁文件、合成输入、可公开结果纳入主仓库，依赖环境不提交。
- 禁止提交密钥、真实业务数据、运行时数据库和未脱敏记录。实验默认用合成数据。
- 不把模型输出当作事实，不把统计贡献当作已证实的因果，不把工程测试通过当作业务分析正确。
- 保留失败、跳过、未验证的结果。不得为提高指标修改测试答案。
- Agent 轨迹记录公开计划、工具输入输出、状态、来源和异常，不收集或展示模型隐藏推理。
- 用户在编排工作流时指定主智能体及其内核，随流程版本保存；运行时不能由调度器或能力预检自动替换。LangGraph、AgentScope 均须能承担主智能体角色，不能把其中一个永久固定为父级。多内核接入的现行候选方案由产品保存共同对象，在框架与产品接触的边界做转换；接口名称、工作流调度归属和具体实现须经实验与评审。
- 应用代码目录先保留边界说明，不提前搭建未经确认的完整产品。
