[project]
name = "xuanyue-kernel-contract-spike"
version = "0.0.1"
description = "Synthetic LangGraph checkpoint and recovery experiment; not application code"
requires-python = ">=3.12,<3.13"
dependencies = [
  "langgraph==1.2.12",
  "langgraph-checkpoint-sqlite==3.1.1",
]

[tool.uv]
package = false
